Polygon-drawing toolbar over `MapSurface` (S-017) — a *pattern*: `MapSurface` region variant + `IconButton` toolbar, not yet packaged (source fix list DF-003).

- Every drawing action has a keyboard shortcut and an equivalent toolbar button.
