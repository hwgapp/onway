Transient confirmation — auto-dismisses after 4s, at most one action.

- `surface-inverse` fill; `aria-live="polite"` (error tone: `assertive`).
- Use `Banner` instead for a notice that should stay on screen.
