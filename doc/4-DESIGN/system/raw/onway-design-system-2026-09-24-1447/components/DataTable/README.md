Dense Admin table — drivers, catalog, regions, cases, audit (S-017, S-019, S-020, S-021).

- Sticky header 36px, row height 44px, numeric columns right-aligned in `numeric`.
- Status uses `StatusPill size="sm"`; row detail opens `AdminDrawer` — never a card inside a row.
