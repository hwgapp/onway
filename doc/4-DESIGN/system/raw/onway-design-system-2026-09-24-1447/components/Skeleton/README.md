Loading placeholder — must match the real content's size exactly.

- `variant="line"` for text rows, `variant="block"` for cards/images.
- Shimmer runs 1.4s; disabled under `prefers-reduced-motion: reduce`. Announce with `role="status"`.
