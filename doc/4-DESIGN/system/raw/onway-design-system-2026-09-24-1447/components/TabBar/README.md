Bottom navigation, 3–4 items — Customer and Driver apps.

- Height `layout-tabbar-height` (60px). Active item uses `text-primary`; inactive uses `text-tertiary`.
- A `Badge` sits on the icon for unread counts (e.g. Hoạt động).
