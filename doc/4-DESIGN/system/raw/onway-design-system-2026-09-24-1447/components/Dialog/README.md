Confirm / critical modal — cancel a ride, lock a driver, publish a region polygon.

- `critical` requires a typed reason before the confirm button enables.
- Focus trap, Esc closes, focus returns to the element that opened it — always.
- Every `destructive` `Button` routes through here; never fires directly.
