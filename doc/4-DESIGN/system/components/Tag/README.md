Selectable filter chip — quick filters on customer search and admin filter bars.

- Toggle `selected` to fill it with `action-secondary-bg`; sets `aria-pressed`.
- Not for status — use `StatusPill` for business state.
