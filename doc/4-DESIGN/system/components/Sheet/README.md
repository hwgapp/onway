Bottom sheet — the primary mobile surface for booking, ride detail, order detail (S-002…S-008, S-013, S-014).

- `radius-sheet` top corners, drag handle 36×4, `shadow-sheet`.
- Handle carries an `aria-label`; content commonly opens with `JobStatusHeader`.
