Payment-proof capture and review (S-004, S-007, S-011, S-013, S-014, S-021).

- States: `empty` (dashed frame), `uploading` (progress bar, blocks a second submit), `failed` (keeps the file locally so retry needs no re-pick), `submitted`, `verified`/`rejected` (driver/admin side, rejection always carries a specific reason).
- The proof image is private media — short-lived signed URL, never cached publicly, never shown outside its job/case.
