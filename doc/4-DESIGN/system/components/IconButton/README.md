Icon-only control for a toolbar or list row.

- `label` is required and renders as `aria-label` (Vietnamese) — an icon with no label never ships.
- Size: 40×40 on web, 44×44 on mobile (matches `touch-target-min`).
- Add a `Tooltip` on web for icon-only actions in Admin.
