Driver–customer / support messaging (S-015, S-021) — a *pattern*, not yet packaged; the source's own fix list (DF-001) flags it as missing spec for retention and failed-upload states.

- Own-message bubbles use `action-secondary-bg`; the other side uses `surface-sunken`.
- An expired attachment renders as muted, italic placeholder text, never a broken image.
