Launcher / store / favicon icon, taken from the circular mark — never the wordmark.

- `variant="customer"` (red background) for the customer app; `variant="driver"` (charcoal background) for the driver app, so the two apps are told apart on a home screen.
- `variant="favicon"` for the browser tab / Admin favicon.
