Pickup/destination search (S-002, S-006) — a *pattern*: `Input` search + `ListRow` results + `Banner` for no-location-permission, not yet its own packaged component in the source.

- Show the no-permission `Banner` in place of results, never an empty list with no explanation.
