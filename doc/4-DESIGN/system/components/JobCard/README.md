Compact trip/order summary card (S-012, S-013, S-014, S-016) — a *pattern*: `Card` + `StatusPill` + `PriceRow`, not yet its own packaged component in the source (see the system README's Not-synced note).

- Never nest another `Card` inside it.
